import React from 'react'
import { FormContainer, Input, Button } from "./styles";

function Form() {
    return (
        <FormContainer><Input />
            <Button>Add</Button>
        </FormContainer>
    )
}

export default Form